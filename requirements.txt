# Core
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
pydantic>=2.0,<3.0

# Database
# Dev: SQLite (built-in)
# Production: PostgreSQL via psycopg2 or asyncpg, or Supabase
sqlalchemy>=2.0,<3.0
psycopg2-binary>=2.9,<3.0
asyncpg>=0.30,<1.0  # for pure async PostgreSQL

# AI / LLM
httpx>=0.28,<1.0     # async HTTP for LLM queries
sse-starlette>=2.0   # Server-Sent Events for streaming AI

# Vector search (semantic search, replaces FTS5)
# pgvector is a PostgreSQL extension — install it on the DB server
# For embeddings generation:
# pip install sentence-transformers numpy

# Search fallback
# pip install pg_trgm  # PostgreSQL trigram extension for fuzzy text search

# Auth
supabase>=2.0,<3.0

# Middleware
python-multipart>=0.0.18

# Production
gunicorn>=23.0,<24.0     # process manager for uvicorn workers

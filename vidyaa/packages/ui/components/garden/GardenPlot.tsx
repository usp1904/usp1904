/** 4.6 GardenPlot — 64 isometric diamonds, stage-aware */
export function GardenPlot({ chapters, onSelect }: { chapters: any[], onSelect: (id:string)=>void }){
  // SVG iso grid — see index.html #isoSvg for full implementation
  return <svg viewBox="0 0 800 420" id="isoSvg" />;
}
